# Application
# One of: development | production | test
NODE_ENV=development
PORT=3000

# PostgreSQL connection string used by Prisma.
# Matches the local database started with `docker compose up -d postgres`
# from the repository root (credentials come from the root .env):
# postgresql://<POSTGRES_USER>:<POSTGRES_PASSWORD>@localhost:<POSTGRES_PORT>/<POSTGRES_DB>?schema=public
DATABASE_URL=postgresql://charityhub:charityhub@localhost:5432/charityhub?schema=public

# Authentication
# Secret used to sign JWTs. At least 16 characters; use a long random value in production.
JWT_SECRET=change-me-to-a-long-random-secret
# Access token lifetime (e.g. 15m, 1h, 7d)
JWT_EXPIRES_IN=1h
